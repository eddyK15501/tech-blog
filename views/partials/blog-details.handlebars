<div class="blog-card">
    <a href="/blog/{{id}}">
        <div class="blog-header">
            <h4>{{title}}</h4>
            <p>Posted by <span>{{user.username}}</span> on {{format_date createdAt}}</p>
        </div>
        <div class="blog-content">
            <p class="blog-main-content">{{content}}</p>
            <div class="num-of-comments">
                <p>0 comments</p>
            </div>
        </div>
    </a>
</div>